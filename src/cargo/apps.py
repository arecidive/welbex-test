from django.apps import AppConfig


class CargoConfig(AppConfig):
    """
    Класс представляет собой конфигурацию приложения 'cargo' в Django.
    """

    default_auto_field = 'django.db.models.BigAutoField'
    name = 'cargo'
