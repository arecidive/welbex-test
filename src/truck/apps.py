from django.apps import AppConfig


class TruckConfig(AppConfig):
    """
    Класс представляет собой конфигурацию приложения 'truck' в Django.
    """

    default_auto_field = 'django.db.models.BigAutoField'
    name = 'truck'
