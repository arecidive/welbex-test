from django.apps import AppConfig


class ApiConfig(AppConfig):
    """
    Класс представляет собой конфигурацию приложения 'api' в Django.
    """

    default_auto_field = 'django.db.models.BigAutoField'
    name = 'api'
