from django.apps import AppConfig


class LocationConfig(AppConfig):
    """
    Класс представляет собой конфигурацию приложения 'location' в Django.
    """

    default_auto_field = 'django.db.models.BigAutoField'
    name = 'location'
